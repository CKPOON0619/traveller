import React from 'react'
import { BrowserRouter } from 'react-router-dom'
import { screen } from '@testing-library/react'

describe('<VisitedCities /> component', () => {
  it('renders the correct loading placeholder', () => {})
  it('renders the correct error placeholder', () => {})
  it('renders the Cards with CityFacts', () => {})
  it('renders the Cards with WishList', () => {})
})
