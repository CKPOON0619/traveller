import React from 'react'
import { BrowserRouter } from 'react-router-dom'
import { screen } from '@testing-library/react'

describe('<CityCard /> component', () => {
  it('renders the correct loading placeholder', () => {})
  it('renders the correct error placeholder', () => {})
  it('renders the Card with CityFacts', () => {})
  it('renders the Card with optional CityUserConfig', () => {})
  it('calls mutation handler with correct data', () => {})
})
